# 已知问题（known-issues）

条目：`### YYYY-MM-DD 标题`，症状/根因/规避。

### 2026-09-18 claude-cli 模式 LLM API 429 限流（5 小时限额）

- 症状：claude-cli 分析模式下批量分析时触发 429 限流（约 5 小时限额），导致分析中断；此前多次记忆总结会话也因 429 空转。
- 规避：优先使用 api 模式（config.json `provider: "api"`）；CLI 模式下控制批量节奏、429 后等待重试。
- 补充（2026-08-26 确认）：每日 ~1:00 定时的记忆总结会话也频繁命中同一 5 小时限额，产生大量空转会话——此类 429 空会话可直接跳过不处理。
